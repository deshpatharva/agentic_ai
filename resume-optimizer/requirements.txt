fastapi
uvicorn
python-docx
pdfplumber
pypdf
numpy<2.0          # thinc (spaCy dep) compiled against numpy 1.x ABI — numpy 2.x breaks it
spacy==3.7.5
# spaCy model pinned as a wheel so Oryx installs it via pip during App Service build.
# `python -m spacy download` only works interactively — it won't run in Oryx.
# Verify the release URL at: https://github.com/explosion/spacy-models/releases
# en_core_web_sm 3.7.x is compatible with spaCy 3.7.x (major.minor must match).
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
scikit-learn
anthropic
litellm
crewai>=1.0.0,<2.0
pysqlite3-binary  # ChromaDB (CrewAI dep) requires sqlite3 >= 3.35.0; Bullseye ships 3.34.x
python-multipart
sse-starlette
python-dotenv
google-genai
groq
# Delta Lake
deltalake
pandas
# Azure Storage
azure-storage-blob>=12.19.0
azure-identity>=1.15.0
pyarrow
# Database
asyncpg                   # PostgreSQL async driver (postgresql+asyncpg:// URL)
sqlalchemy[asyncio]       # asyncio extra adds greenlet, required by create_async_engine
bcrypt                    # password hashing in auth/router.py
# Database migrations
alembic>=1.13.0
email-validator
python-jose[cryptography]
# Job scraper
httpx
slowapi>=0.1.9
python-json-logger>=2.0.7
gunicorn
# Optional: boto3 for S3 Delta storage in prod
# boto3
