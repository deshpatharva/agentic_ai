# Normalize all text files to LF in the repo; git auto-detects binaries.
* text=auto eol=lf

# Windows-specific scripts need CRLF to run correctly.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
